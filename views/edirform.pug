center EDIT- 
  div=  data.title
form(action=`/blogs/${data._id}?_method=PUT` method="POST")
    label Title 
       input(type="text" name="title" value=`${data.title}`)
    lable Body
       input(type="textbox" name="body" value=`${data.body}`)
    label Image 
       input(type="text" name="image" value=`${data.image}`)

    button(type="submit" class="ui ") SUBMIT
